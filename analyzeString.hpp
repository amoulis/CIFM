#ifndef ANALYZE_STRING_HPP
#define ANALYZE_STRING_HPP

#include <iostream>
#include <string>

class analyzeString {
	
	private:
		std::string data;

	public:
		analyzeString(std::string nData);
		std::string getString();
};

#endif